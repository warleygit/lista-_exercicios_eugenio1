let numero1 = 20;  // número que queremos verificar
let numero2 = 5;   // possível múltiplo

// Verifica se numero1 é múltiplo de numero2
if (numero1 % numero2 === 0) {
  console.log(numero1 + " é múltiplo de " + numero2);
} else {
  console.log(numero1 + " não é múltiplo de " + numero2);
}